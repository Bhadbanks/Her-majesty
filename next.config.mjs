// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure the project builds successfully
  reactStrictMode: true,
  
  // Optional: Add headers if needed for Vercel deployment, but default is fine.
};

export default nextConfig;
